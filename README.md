# 🧾 QR Code Generator

A simple Python program to generate QR codes from text or URLs.  
The generated QR image can be saved as a `.png` file on your system.

## 🚀 Features
- Convert any text or URL into a QR code  
- Saves QR code as an image (`.png`)  
- Allows custom file name for saving  
- Supports quick and offline generation  

## 🧠 How It Works
The script uses the **qrcode** Python library to generate and save QR codes locally.

# 🛠️ Built With
Python
<br>
qrcode library
<br>
PIL (Python Imaging Library)

## ⚙️ Requirements
Make sure Python is installed on your system, then install the required library:
```bash
pip install qrcode[pil]

🖥️ Usage
Clone or download the repository:
Navigate into the folder:
python qr_generator.py
Enter a text or URL when prompted, then enter a file name (or press Enter to use the default my_qr.png).
```
# 👨‍💻 Author
Developed by Dip Mondal
If you’d like to connect, reach me at dipmondal19564@gmail.com
